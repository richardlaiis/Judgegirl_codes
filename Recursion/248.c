#include <stdio.h>
#define SIZE 9
int main() {
    int cnt[SIZE][SIZE];
    for(int i = 0; i < SIZE; i++) {
        for(int j = 0; j < SIZE; j++) {
            scanf("%d", &cnt[i][j]);
        }
    }
    
}