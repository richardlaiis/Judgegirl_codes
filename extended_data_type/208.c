#include <stdio.h>
int main() {
	unsigned int width,height;
	scanf("%u%u",&width,&height);
	char sign[2], ans[width*height];
	scanf(" %c %c",&sign[1],&sign[0]);
	//printf("%c %c\n",sign[0],sign[1]);
	while(scanf("%u",&num)!=EOF) {

	}
		
}
